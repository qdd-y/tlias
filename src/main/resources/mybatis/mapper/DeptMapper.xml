<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.qdd.tlias.mapper.DeptMapper">
    <insert id="insert">
        insert into
            dept(name,create_time,update_time)
        values(#{name},#{createTime},#{updateTime})
    </insert>
    <update id="update">
        update dept set name=#{name},update_time=#{updateTime} where id=#{id}
    </update>

    <delete id="delete">
        delete from dept where id=#{id}
    </delete>

    <select id="countEmpByDeptId" resultType="java.lang.Integer">
        select count(*) from emp where dept_id = #{deptId}
    </select>

    <select id="findAll" resultType="cn.qdd.tlias.pojo.Dept">
        select * from dept
    </select>
    <select id="getById" resultType="cn.qdd.tlias.pojo.Dept">
        select * from dept where id=#{id}
    </select>


</mapper>